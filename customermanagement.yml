# rename this file to customermanagement.yml and put a ':' after 'name' if
# you want to use YAML like in old versions of Catalyst
name: CustomerManagement
'View::JSON':
      expose_stash: json_data

